services:
  - type: web
    name: vcat-bond-bundle-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn vcat_bond_bundle.app.main:app --host 0.0.0.0 --port $PORT
